# 2026-03-08 네이버 티켓 작업 요약

### TODO-01. 모바일 티켓 공개 정책 2시간 전 통일

- ✅ 공연 생성 화면과 네이버 위자드에서 모바일 티켓 공개 시점을 공연 시작 2시간 전으로 통일한다.
- ✅ 공개 티켓 화면이 이벤트별 `revealAt` 값을 우선 사용하도록 맞춘다.
- ✅ 공개 전에는 좌석과 QR이 모두 숨겨지고, 서버에서도 QR 발급을 막는다.

**완료 기준**
- 공개 정책이 어드민, 사용자 앱, 서버에서 같은 기준으로 동작한다.
- 공개 전 티켓은 UI만 아니라 서버에서도 보호된다.

**진행 상태**
- 2026-03-08 완료
- 이벤트 생성 로직의 기본 공개 시점을 1시간 전에서 2시간 전으로 조정
- 공개 티켓 화면이 `event.revealAt` 기준으로 좌석 공개와 QR 노출 시점을 계산하도록 정리
- QR 발급 API가 공개 전 요청을 `failed-precondition`으로 차단하도록 보강

### TODO-02. 그룹 티켓 허브와 전달 동선 정리

- ✅ 2장 이상 주문은 공개 링크 진입 시 그룹 티켓 허브를 먼저 보여준다.
- ✅ 각 티켓마다 `티켓 보기`와 `전달하기`를 분리한다.
- ✅ 개별 상세에서 다시 전체 티켓 목록으로 돌아갈 수 있게 한다.

**완료 기준**
- 여러 장 구매한 사용자가 스와이프만으로 헤매지 않는다.
- 그룹 주문에서도 전달과 확인 흐름이 분리된다.

**진행 상태**
- 2026-03-08 완료
- 그룹 허브를 추가해 티켓별 상태, 이름, 좌석 공개 상태, 액션을 한 화면에서 보도록 정리
- 티켓별 `전달하기`와 `티켓 보기`를 분리하고, 상세 상단에 `전체 티켓` 복귀 동선을 추가
- 받는 사람 이름 수정이 허브와 상세 양쪽에 즉시 반영되도록 상태 동기화

### TODO-03. 공개 모바일 티켓을 이름 중심 구조로 재정리

- ✅ 티켓 앞면은 공유용, 뒷면은 본인 확인용으로 역할을 나눈다.
- ✅ 전달된 티켓은 받는 사람 이름이 가장 먼저 보이게 한다.
- ✅ 예매자, 받는 사람, 예매번호, 티켓번호의 우선순위를 다시 정리한다.

**완료 기준**
- 사용자가 링크를 열면 누구 티켓인지 바로 이해할 수 있다.
- 본인 확인 정보와 공유용 정보가 섞이지 않는다.

**진행 상태**
- 2026-03-08 완료
- 티켓 앞면을 이름 중심 레이아웃으로 재구성하고 `예매자`, `받는 사람`, `전달 완료` 배지를 추가
- 뒷면에 `본인 확인 정보` 블록을 구성해 예매자, 받는 사람, 예매번호, 티켓번호를 분리
- 전화번호는 뒷면에서만 마스킹 형식으로 노출하도록 정리

### TODO-04. 모바일 티켓 공유 경험과 OG 미리보기 정리

- ✅ `티켓 공유`, `전달하기`, `친구 초대` 문구와 역할을 분리한다.
- ✅ 카카오톡/링크 미리보기용 OG 메타를 공연명, 일시, 장소 중심으로 정리한다.
- ✅ `이미지 저장`은 인스타 업로드에 맞는 전용 비율로 만든다.

**완료 기준**
- 링크만 보내도 공연 정보가 바로 보인다.
- 저장 이미지는 홍보용으로 바로 사용할 수 있다.

**진행 상태**
- 2026-03-08 완료
- 공유 문구를 공연명, 일시, 장소, 링크 중심으로 통일
- OG 메타에 description, image alt, noindex, 캐시 정책을 추가해 미리보기 품질을 보강
- 저장 이미지를 카드 캡처가 아닌 4:5 인스타 업로드용 레이아웃으로 교체

### TODO-05. 현장 체크인 운영 마감

- ✅ 공개 전, 사용 완료, 취소됨 상태가 스캐너와 티켓 화면에서 같은 의미로 보이도록 맞춘다.
- ✅ 입장 시 QR 스캔 후 결과가 티켓 화면에 다시 반영되도록 정리한다.
- ✅ 인터미션은 화면 확인 중심이라는 운영 정책을 문서와 UI에 반영한다.

**완료 기준**
- 현장 스태프와 관객이 서로 다른 상태 용어를 보지 않는다.
- 공개 전 티켓과 사용 완료 티켓은 다시 잘못 사용되지 않는다.

**진행 상태**
- 2026-03-08 완료
- 상태 용어를 `공개 전 / 사용 가능 / 입장 완료 / 사용 완료 / 취소됨` 기준으로 통일
- 티켓 화면과 그룹 허브에 `상태 새로고침` 동선을 추가해 체크인 결과를 다시 확인 가능하게 정리
- 공개 전과 사용 완료 상태에서는 QR 재발급을 막아 서버와 UI가 같은 정책을 따르도록 보강
- 스캐너와 티켓 화면에 `인터미션은 화면 확인 중심, 필요 시만 재입장 QR 스캔` 정책을 반영

### TODO-06. 운영 도구 보강

- ✅ 어드민에서 공개 상태, 티켓 상태, 전달 현황, 문자 현황을 한 번에 보게 만든다.
- ✅ 운영 메모와 정책 문구를 화면에 명시해 현장 혼선을 줄인다.
- ✅ 예외 주문도 콘솔 없이 확인 가능한 수준으로 정리한다.

**완료 기준**
- 운영자가 주문 목록과 티켓 상태를 빠르게 판단할 수 있다.
- revealAt 정책과 문자/전달 상태가 한 화면에 드러난다.

**진행 상태**
- 2026-03-08 완료
- 주문 관리 상단에 `OPERATIONS` 요약 카드 섹션을 추가
- 공개 상태, 자동 공개까지 남은 시간, 티켓 상태 집계, 전달 현황, 문자 발송 상태를 한 번에 표시
- 공연 등록 위자드에도 `공연 시작 2시간 전 자동 공개, 필요 시 주문 화면에서 즉시 공개` 정책 메모 추가

### TODO-07. 봇 연동과 자동화 안정화

- ✅ 네이버 주문 생성 로직을 callable과 HTTP에서 공통화한다.
- ✅ 네이버 주문 취소 로직도 같은 기준으로 공통화한다.
- ✅ 봇 인증키 fallback을 제거하고 설정 누락을 명확히 드러내게 한다.
- ✅ 이벤트 목록 정렬 기준을 `startAt`으로 맞춘다.

**완료 기준**
- 주문 생성/취소 정책이 진입 경로에 따라 달라지지 않는다.
- 봇 설정 누락이 조용히 묻히지 않는다.

**진행 상태**
- 2026-03-08 완료
- `createNaverOrderInternal`, `cancelNaverOrderInternal`로 생성/취소 로직을 통합
- 봇 인증을 `requireBotRequestAuth`로 공통화하고 하드코딩된 API 키 fallback 제거
- `listEventsHttp`가 레거시 `date` 대신 `startAt` 기준으로 정렬되도록 수정
- 놀티켓 마감 완료와 미판매 좌석 업로드 완료 후 자동 배정 시작이라는 운영 기준을 문서화

### TODO-08. 테스트 최소 안전망 만들기

- ✅ Functions 쪽 공개 payload, 주문 생성 입력, 취소 정책 테스트를 추가한다.
- ✅ Flutter 쪽 공개 티켓 상태와 그룹 허브 진입 규칙 테스트를 추가한다.
- ✅ 최소 회귀선이 깨졌을 때 바로 잡을 수 있도록 한다.

**완료 기준**
- 공개 정책, 상태 정책, 그룹 티켓 진입 규칙 변경 시 기본 회귀 검증이 가능하다.

**진행 상태**
- 2026-03-08 완료
- 순수 로직 모듈을 분리해 DB 없이 검증 가능한 Functions 테스트 6건 추가
- 모바일 티켓 상태 계산과 그룹 허브 초기 진입 규칙을 helper로 분리하고 Flutter 테스트 7건 추가
- `npm test`, `npm run build`, `flutter test`, `flutter analyze` 기준으로 기본 회귀선 검증 완료

### MT-068. 네이버 구매 이력과 사용자 계정 매핑

- ✅ 사용자가 `주문번호 + 연락처`로 네이버 주문을 자기 계정에 연결할 수 있게 한다.
- ✅ 앱에서 연결된 네이버 주문 목록을 볼 수 있게 한다.
- ✅ 어드민에서 앱 연결 여부를 바로 확인할 수 있게 한다.

**완료 기준**
- 사용자가 네이버 주문을 자기 계정으로 안전하게 연결할 수 있다.
- 운영자가 앱 연결 상태를 주문 목록에서 바로 확인할 수 있다.

**진행 상태**
- 2026-03-08 완료
- `claimNaverOrder` callable을 추가해 네이버 주문 self-claim 흐름 구현
- 앱에 `네이버 예매 내역` 화면과 `주문 연결` 다이얼로그를 추가
- 어드민 주문 목록에 `앱 연결됨 / 앱 미연결` 배지를 추가
- 같은 계정 재연결은 성공 처리하고, 다른 계정에 이미 연결된 주문은 차단

### MT-066 ~ MT-075. 운영 런북과 오픈 기준 정리

- ✅ 트랙 A 실운영 체크리스트를 문서화한다.
- ✅ 예외 대응표, 스태프용 운영 가이드, Go / No-Go 기준을 정리한다.
- ✅ 트랙 B 계정 연동, 좌석 선택, 결제, 시야 이미지, 정산 기준을 문서화한다.
- ✅ 브랜드와 정식 오픈 준비 기준을 정리한다.

**완료 기준**
- 다음 세션에서도 문서만 보면 운영, 확장, 오픈 준비를 이어갈 수 있다.

**진행 상태**
- 2026-03-08 완료
- `네이버 티켓 운영 런북.md`를 새로 작성해 공연 전날, 공연 당일, 공개 시점, 입장, 인터미션, 종료 후 체크리스트를 정리
- 예외 대응표, 스태프용 한 장 가이드, Go / No-Go 기준을 추가
- 네이버 로그인 기준, 주문 매핑 기준, 좌석 선택형 데이터 truth table, 고령층 UX 점검표, 브랜드 정리 항목을 문서화
- 마스터플랜과 런북을 연결해 제품 기준 문서와 실운영 기준 문서를 분리

### 텔레그램 전송 준비

- ✅ 날짜 포함 요약본을 별도 Markdown 파일로 저장한다.
- ✅ 텔레그램 전송 스크립트가 progress 로그뿐 아니라 임의의 요약 파일도 보낼 수 있게 준비한다.
- ✅ 실제 봇 토큰과 채팅 ID를 받아 전송한다.

**완료 기준**
- 토큰을 받으면 요약 파일을 바로 텔레그램으로 전송할 수 있다.

**진행 상태**
- 2026-03-08 완료
- 전송용 요약본을 `scripts/ralph/telegram-summary-latest.md`로 저장
- `node scripts/ralph/send-telegram-summary.js --file scripts/ralph/telegram-summary-latest.md` 형태로 바로 보낼 수 있게 스크립트 확장
- 실제 발송은 `RALPH_TELEGRAM_BOT_TOKEN`, `RALPH_TELEGRAM_CHAT_ID` 또는 기존 대체 환경변수 입력 후 진행 예정
